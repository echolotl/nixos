# echolotl's NixOS configs

This is the repo where I store my NixOS configs. It contains branches for my desktop (`nixo`) and my laptop (`nixt`).

I don't think I especially use Nix correctly in the way that NixOS is setup, as I mostly just use it like a package manager (even though it should be per project? idk)